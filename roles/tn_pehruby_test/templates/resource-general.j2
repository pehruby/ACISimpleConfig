{
    "{{resourcetype}}": {
        "attributes": {
{% for (key,value) in item.attributes.items() %}             
            "{{key}}": "{{value}}" {% if not loop.last %},{% endif %}
{% endfor %}
        }
    }
}